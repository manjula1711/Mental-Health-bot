# Flask for web application
Flask
Flask-Cors  # To handle CORS issues if needed

# NLP preprocessing
nltk
numpy
pandas
scikit-learn  # For encoding labels and text processing

# Deep Learning (ANN, LSTM, GRU)
tensorflow  # For neural network model
keras       # Keras API for TensorFlow models

# Vectorization and Embeddings (if used)
sentence-transformers  # For word embeddings (if needed)

# Additional Utilities
gunicorn  # For production deployment
python -c "import nltk; nltk.download('punkt'); nltk.download('punkt_tab')"
